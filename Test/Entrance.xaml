<Window x:Class="Test.Entrance"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test"
        mc:Ignorable="d"
        Title="Вход" Height="287" Width="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label FontSize="20" FontWeight="Bold" Grid.Row="0" Content="Вход" HorizontalAlignment="Center"/>
        <WrapPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Center">Логин: </TextBlock>
            <TextBox x:Name="tbLogin" Height="20" Width="200"></TextBox>
            <TextBlock HorizontalAlignment="Center">Пароль:</TextBlock>
            <PasswordBox x:Name="tbPass" Height="20" Width="200" />
            <WrapPanel HorizontalAlignment="Center">
                <Button Margin="0,10,0,0" Click="Button_Click"
                        Padding="30,10,30,10" IsDefault="True">Войти</Button>
            </WrapPanel>
        </WrapPanel>
    </Grid>
</Window>
